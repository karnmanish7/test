@model TaskRobo.Models.Category
@{
    ViewBag.Title = "Create";
}

@*<h2>Create</h2>*@

<hr />


@using (Html.BeginForm())
    {
        @Html.ValidationSummary(true, "create Failed, fields.");
        <div>
            <fieldset>
                <legend>Category Form</legend>
                <div class="editor-label">@Html.LabelFor(u => u.CategoryTitle)</div>
                <div class="editor-field">
                    @Html.TextBoxFor(u => u.CategoryTitle)
                    @Html.ValidationMessageFor(u => u.CategoryTitle)
                </div>
                <input type="submit" value="Create" />
            </fieldset>
        </div>
    }
    @*<hr />*@


@*<div class="row">
    <div class="col-md-4">
        <form asp-controller="Categories" asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="CategoryTitle" class="control-label"></label>
                <input asp-for="CategoryTitle" class="form-control" />
                <span asp-validation-for="CategoryTitle" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>*@
